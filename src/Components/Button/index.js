import styled from 'styled-components';

export const Button = styled.button`
    cursor: pointer;
    width: 100%;
    height: auto;
    padding: 10px 25px;
    font-size: 1.1rem;
    font-weight: bold;
    color: #cccc;
    border-radius: 25px;
    
`;

export default Button;